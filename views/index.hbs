<div class="row mt-4">
  {{#if receitas.length}}
    {{#each receitas}}
    <div class="col-md-4 mb-4">
      <div class="card h-100">
        <div class="card-body">
          <h5 class="card-title">{{this.titulo}}</h5>
          <p class="card-text">Tempo de preparo: {{this.tempo}} minutos</p>
        </div>
        <div class="card-footer bg-transparent border-top-0">
          <a href="/receita/{{this.id}}" class="btn btn-info btn-sm text-white w-100 mb-1">
            <i class="bi bi-eye"></i> Ver Detalhes
          </a>
          <div class="d-flex gap-1">
            <a href="/receita/editar/{{this.id}}" class="btn btn-warning btn-sm flex-fill">
                <i class="bi bi-pencil"></i> Alterar
            </a>
            <a href="/receita/excluir/{{this.id}}" class="btn btn-danger btn-sm flex-fill">
                <i class="bi bi-trash"></i> Excluir
            </a>
          </div>
        </div>
      </div>
    </div>
    {{/each}}
  {{else}}
    <div class="col-12 text-center mt-5">
      <div class="alert alert-warning" role="alert">
        <h4 class="alert-heading">Nenhuma receita cadastrada!</h4>
        <p>Comece a usar o sistema cadastrando sua primeira receita culinária.</p>
        <hr>
        <a href="/receita/nova" class="btn btn-primary">Cadastrar Nova Receita</a>
      </div>
    </div>
  {{/if}}
</div>
