<div class="row mt-4">
  {{#if receitas.length}}
    {{#each receitas}}
      <div class="col-md-4 mb-4">
        <div class="card h-100 shadow-sm">
          <div class="card-body">
            <h5 class="card-title">{{this.titulo}}</h5>
            <p class="card-text"><i class="bi bi-clock"></i> {{this.tempo}} minutos</p>
          </div>
          <div class="card-footer bg-transparent border-top-0">
            <a href="/receita/{{this.id}}" class="btn btn-info btn-sm text-white w-100 mb-2">
              <i class="bi bi-eye"></i> Ver Detalhes
            </a>
            <div class="d-flex gap-2">
              <a href="/receita/editar/{{this.id}}" class="btn btn-warning btn-sm flex-fill">
                <i class="bi bi-pencil"></i> Alterar
              </a>
              <button type="button" class="btn btn-danger btn-sm flex-fill" data-bs-toggle="modal" data-bs-target="#deleteModal{{this.id}}">
                <i class="bi bi-trash"></i> Excluir
              </button>
            </div>
          </div>
        </div>
      </div>

      <div class="modal fade" id="deleteModal{{this.id}}" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
        <div class="modal-dialog">
          <div class="modal-content">
            <div class="modal-header">
              <h5 class="modal-title">Excluir Receita</h5>
              <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Tem certeza que deseja excluir a receita <strong>"{{this.titulo}}"</strong>?
            </div>
            <div class="modal-footer">
              <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
              <a href="/receita/excluir/{{this.id}}" class="btn btn-danger">Confirmar Exclusão</a>
            </div>
          </div>
        </div>
      </div>
    {{/each}}
  {{else}}
    <div class="col-12 text-center mt-5">
      <div class="alert alert-warning" role="alert">
        <h4 class="alert-heading">Nenhuma receita cadastrada!</h4>
        <p>Comece a usar o sistema cadastrando sua primeira receita culinária.</p>
        <hr>
        <a href="/receita/nova" class="btn btn-primary">Cadastrar Nova Receita</a>
      </div>
    </div>
  {{/if}}
</div>
